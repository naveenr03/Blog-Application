# Copy to ".env" in blog-frontend/ if you need local overrides (file is gitignored).

# Production / preview builds: set this on the host (Vercel, Netlify, etc.) — not required for `npm run dev`.
# Dev uses relative "/api/v1" and the Vite proxy to localhost:8080.
# VITE_API_BASE_URL=https://your-api.onrender.com/api/v1
